<!DOCTYPE html>
<html>
<head>
  <title>RailsVChat</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">


       <div class="row">
            <div class="col-lg-12">
    <nav class="navbar navbar-inverse navbar-default navbar-fixed-top" role="navigation">

        <div class="container">

            <div class="navbar-header">

                <!-- Przycisk, który pojawia sie na urzadzeniach mobilnych lub przy zminimalizowanej przegladarce  -->
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".MenuCollapse">

                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
      <a class="brand" href="#">
                   <i style="padding-right:40px;" class="glyphicon glyphicon-facetime-video"></i>
                </a>


            </div>
            <div style="font-size:40px;"> <%= link_to "RailsVChat", '#', id: "logo" %></div>
            <div class="collapse navbar-collapse MenuCollapse">
               <!-- <%=link_to "Label", :controller => :rooms, :action => :index %>> -->
                <ul id="menu" class="nav navbar-nav navbar-right">
                <li><%= link_to 'Poczekalnia', root_path%> </li>
        			<li id="logo"><%=@user_name%> </li>
                    <li id="io">
                    		<%if user_signed_in? %>
								<%= link_to "Wyloguj", destroy_user_session_path, :method => :delete %>
							<% else %>
								<%= link_to "Logowanie", new_user_session_path %>
							<% end %>
                    </li>               
                </ul>
            </div>
    
        </div>
      
    </nav>
</div>
</div>

      

<% flash.each do |key, value| %>
        <div class="<%= flash_class(key) %>">
              <%= value %>
        </div>
  <% end %>

<%= yield %>

</div>

</body>
</html>
